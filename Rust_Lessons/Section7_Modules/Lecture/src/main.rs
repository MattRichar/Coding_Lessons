//There are a number of features that Rust has to organize code.

//Packages allow us to build test, and share crates.
//Package is defined with the Cargo.toml file
//To create a library from the command line, type the command "cargo new <name> --lib"

//Crates are a tree of modules, that produces a library or an executable.
//Crates are about code sharing between projects.

//Modules let us control the organization, scope, and privacy of paths.
//Modules are about code sharing within a project.

//Paths are a way of naming an item, such as a struct, function, or module.


fn main() {

}
